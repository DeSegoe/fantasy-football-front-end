import { PlayerAttributeSummary } from './player-attribute-summary';

describe('PlayerAttributeSummary', () => {
  it('should create an instance', () => {
    expect(new PlayerAttributeSummary()).toBeTruthy();
  });
});
